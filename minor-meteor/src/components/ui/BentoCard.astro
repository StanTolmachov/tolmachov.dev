---
interface Props {
  class?: string;
  accent?: boolean;
}
const { class: className = '', accent = false } = Astro.props;
---

<div class:list={[
  'card p-6 flex flex-col gap-3',
  accent && 'border-[rgba(0,173,216,0.2)] bg-[rgba(0,173,216,0.03)]',
  className
]}>
  <slot />
</div>
